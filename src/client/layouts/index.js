// ============================================================================
// Layouts Module — Barrel Export (PulseOps V2)
//
// PURPOSE: Single entry point for all global layout components.
// Import from '@layouts' — never from deep relative paths.
//
// USAGE:
//   import { AppShell, TopMenu, LeftSideNavBar } from '@layouts';
// ============================================================================

export { default as AppShell } from '@layouts/AppShell';
export { default as TopMenu } from '@layouts/TopMenu';
export { default as LeftSideNavBar } from '@layouts/LeftSideNavBar';
export { default as RightLogsView } from '@layouts/RightLogsView';
export { default as MainContent } from '@layouts/MainContent';
